return {
    "rcarriga/nvim-dap-ui",
    -- dependencies = { "mfussenegger/nvim-dap" },
    -- config = function()
    --     local dap = require("dap")
    --     local dapui = require("dapui")
    --     dapui.setup()
    --     dap.listeners.after.event_initialized["dapup_config"] = function()
    --         dapui.open()
    --     end
    --     dap.listeners.after.event_terminated["dapup_config"] = function()
    --         dapui.close()
    --     end
    --     dap.listeners.after.event_exited["dapup_config"] = function()
    --         dapui.close()
    --     end
    -- end,
}
