return {
  paddings = 3,
  group_paddings = 5,

  -- Input sources, wifi, volume, battery, etc
  icons = "sf-symbols",
  
  -- SF Fonts
  font = {
    text = "SF Pro",     -- Used for text
    numbers = "SF Mono", -- Used for numbers
    style_map = {
      ["Regular"] = "Regular",
      ["Semibold"] = "Semibold",
      ["Bold"] = "Bold",
      ["Heavy"] = "Heavy",
      ["Black"] = "Black",
    }
  },
}
